import * as React from 'react';
import Component from './styles';
import { {{ properCase name }}Props } from './types';

export default function {{ properCase name }}({
  children,
}: {{ properCase name }}Props): JSX.Element {
  return (
    <Component>
      {children}
    </Component>
  );
};
