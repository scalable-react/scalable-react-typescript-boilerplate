import * as React from 'react';
import Component from './styles';
import { {{ properCase name }}Props } from './types';

class {{ properCase name }} extends React.Component<{{ properCase name }}Props, undefined> {
  public render() {
    const {
      children,
    } = this.props;
    return (
      <Component>
        {children}
      </Component>
    );
  }
}

export default {{ properCase name }};
